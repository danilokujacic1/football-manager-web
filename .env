# Base URL for the backend API. Override per-environment as needed.
# Must be NEXT_PUBLIC_* so it is available to client components (axios runs in the browser).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001/api
